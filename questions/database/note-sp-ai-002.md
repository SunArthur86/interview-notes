---
id: note-sp-ai-002
difficulty: L2
category: database
subcategory: MySQL
tags:
- Shopee
- 面经
- MySQL
- 索引
feynman:
  essence: B树每个节点存key+data，B+树只在叶子存data+叶子间有链表——查询更稳定、范围扫描更高效
  analogy: B树像超市货架——每层都摆商品(中间也有)。B+树像图书馆——书都在书架(叶子)，目录(非叶子)只指路，书架间走廊连通(链表)
  first_principle: 数据库查询两种模式：单点查找+范围扫描，B+树为两者都提供最优结构
  key_points:
  - B树非叶子也存data，B+树只在叶子存data
  - B+树叶子节点间有链表，范围扫描极快
  - B+树查询路径稳定(总是到叶子)
  - B+树fanout更大(非叶子不存data)
first_principle:
  essence: 非叶子节点不存data可以增加fanout，降低树高，减少磁盘IO
  derivation: 磁盘页16KB→不存data每页放1170个key→存data每页放16个→树高差3倍→IO次数差3倍
  conclusion: B+树通过牺牲非叶子节点的data存储来换取更高的fanout和更低的树高
follow_up:
- B+树的叶子链表是双向还是单向？
- B+树的分裂策略是什么？
- 为什么不用红黑树或AVL树做索引？
memory_points:
- 数据分布：B树所有节点均存Key+Data，而B+树仅叶子节点存Data，非叶子只存索引Key
- 查询稳定性：B树查询可能在非叶子提前命中故不稳定，而B+树必须查到叶子节点故极其稳定
- 范围查询优势：B树中序遍历回溯产生随机IO，而B+树顺着叶子链表直接做顺序IO极速扫描
- Fanout差异：因为B+树非叶子不存Data，单页能容纳更多Key（约1170个），树高更矮，磁盘IO更少
---

# B树和B+树的区别？

## 结构对比图

```
B树 (B-Tree):
           [30|DATA]              ← 非叶子也有DATA
          /         \
    [10|D, 20|D]    [40|D, 50|D]  ← 每个key都带DATA
    /    |    \     /    |    \
  [5|D] [15|D] [25|D] [35|D] [45|D]
  
  特点：查找可能在非叶子命中返回


B+树 (B+Tree):
           [30]                    ← 非叶子只有KEY
          /     \
    [10, 20]     [40, 50]          ← 非叶子只有KEY(索引)
    /  |  \       /  |  \
  [5][10][15] ←→ [20][25] ←→ [30][35] ←→ [40][45][50]
                   ↑                     ↑
              叶子存DATA             叶子间链表连接
  
  特点：查找总是到叶子；叶子链表支持范围扫描
```

## 四大核心区别

| 区别 | B树 | B+树 |
|------|-----|------|
| **数据位置** | 所有节点都存key+data | 只有叶子存data |
| **查询稳定性** | 不稳定(可能中间命中) | 稳定(总是到叶子) |
| **范围查询** | 中序遍历(随机IO) | 叶子链表(顺序IO) |
| **fanout** | 小(key+data占空间) | 大(只key，每页更多) |

## 详细分析

### 1. 数据存储位置

```
B树：找key=10
  → 根节点[30]比较 → 左子节点[10,20] → 命中key=10 → 直接返回DATA
  → 可能只需2层就拿到数据

B+树：找key=10  
  → 根节点[30] → 左子节点[10,20] → 继续到叶子[5,10] → 返回DATA
  → 总是要走到叶子
```

### 2. 查询稳定性

```
B树：
  查key=10 → 第2层命中(快)
  查key=50 → 第3层命中(慢)
  → 响应时间不稳定

B+树：
  无论查什么key → 都到叶子节点
  → 响应时间稳定一致
  → 便于系统性能预估和SLA保证
```

### 3. 范围查询（关键优势）

```sql
-- 查询 age BETWEEN 20 AND 40

B树的范围查询：
  1. 找到age=20 → 获取data
  2. 中序遍历 → 回到父节点 → 下一个子节点
  3. 每次跳转 = 磁盘随机IO
  → 效率差(大量随机IO)

B+树的范围查询：
  1. 找到age=20的叶子节点
  2. 顺着链表向右扫描
  [20]→[22]→[25]→[28]→[30]→[35]→[40]→stop
  3. 顺序IO → 预读优化 → 极快
```

### 4. fanout与树高

```
假设磁盘页16KB，key=8B，指针=6B，data=1KB

B树每页：
  (key + data + 指针) = (8 + 1024 + 6) ≈ 1KB/条
  → 每页放 ~16 条
  → 100万数据树高 ≈ 4层 → 4次IO

B+树非叶子每页：
  (key + 指针) = (8 + 6) = 14B/条
  → 每页放 ~1170 条
  → 100万数据树高 ≈ 2层 → 2次IO(根节点常驻内存)
```

## 适用场景

| 数据结构 | 适用场景 | 代表产品 |
|---------|---------|---------|
| **B树** | 单点查询多、随机访问多 | MongoDB、文件系统 |
| **B+树** | 范围查询多、OLTP场景 | MySQL、PostgreSQL |

> MongoDB选B树的原因：MongoDB以单文档查询为主(WHERE _id=xxx)，很少做范围扫描。

## 面试加分点

1. **四点概括**：数据位置、查询稳定性、范围查询、fanout
2. **fanout计算**：能估算16KB页可放1170个key(只存key)
3. **范围查询是关键**：B+树叶子链表让范围扫描变顺序IO，这是数据库选B+树的核心原因
4. **MongoDB对比**：B树适合KV查询，B+树适合范围查询

## 记忆要点

- 数据分布：B树所有节点均存Key+Data，而B+树仅叶子节点存Data，非叶子只存索引Key
- 查询稳定性：B树查询可能在非叶子提前命中故不稳定，而B+树必须查到叶子节点故极其稳定
- 范围查询优势：B树中序遍历回溯产生随机IO，而B+树顺着叶子链表直接做顺序IO极速扫描
- Fanout差异：因为B+树非叶子不存Data，单页能容纳更多Key（约1170个），树高更矮，磁盘IO更少

