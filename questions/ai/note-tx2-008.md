---
id: note-tx2-008
difficulty: L4
category: ai
subcategory: 系统设计
tags:
- 腾讯
- 面经
- 生产Agent
- 系统设计
- 架构
feynman:
  essence: 设计生产可用 Agent 系统从0到1搭，按"接入层→编排层→能力层→数据层→可观测层"五层架构。接入层(API网关/鉴权/限流)，编排层(LangGraph状态机+Planning)，能力层(Tool/Memory/LLM)，数据层(Redis短期+Postgres日志+向量库知识)，可观测层(trace/metrics/log)。生产关注点：高可用(熔断降级)、成本(token预算)、安全(权限/审计)、可演进(版本/灰度)。
  analogy: 像建一座工厂——接单处(接入层)、生产调度室(编排层)、车间和仓库(能力层+数据层)、监控中心(可观测层)。不能只有车间没调度，也不能只生产不监控。
  first_principle: 生产 Agent 区别于 demo 的本质是"可运维"。demo 只要能跑通，生产要考虑高可用/成本/安全/可演进。架构分层是为了每层独立演进、独立扩缩容、独立容错。
  key_points:
  - '五层架构: 接入(网关)→编排(状态机)→能力(Tool/Memory/LLM)→数据(Redis/PG/向量)→可观测(trace)'
  - '高可用: 熔断/降级/重试/兜底模型'
  - '成本: token预算/小模型决策+大模型总结/缓存'
  - '安全: 工具权限分级/审计日志/PII脱敏'
  - '可演进: 模型版本/灰度发布/A-B测试'
first_principle:
  essence: 生产 Agent = 可运维的分层架构
  derivation: demo只要跑通 → 生产要可运维 → 分层独立演进/扩缩容/容错 → 每层关注点分离 → 高可用+成本+安全+可演进
  conclusion: 生产 Agent 的难度不在"智能"，而在"工程化"——把 demo 变成 7x24 稳定运行的服务
follow_up:
- Agent 系统怎么做熔断降级？
- token 成本怎么控制和优化？
- 怎么做 Agent 的版本管理和灰度发布？
memory_points:
- 五层架构：接入网关、LangGraph编排、模型与工具能力、多模态存储数据、Trace可观测
- 编排核心：状态机控制流转，Plan&Execute分离规划与执行，硬步数上限防死循环
- 生产高可用：LLM与工具需配熔断降级，大模型挂了降级小模型或缓存，最终退化为规则
- 成本与安全：小模型做大模型意图识别省钱，写入工具需二次确认防注入
---

# 【某讯面经】设计一个生产可用的 Agent 系统，从0到1怎么搭？

## 一、五层架构

```
┌─────────────────────────────────────────────────────────────┐
│  [可观测层] trace / metrics / log / 告警                     │
├─────────────────────────────────────────────────────────────┤
│  [接入层] API网关 / 鉴权 / 限流 / 协议转换                    │
├─────────────────────────────────────────────────────────────┤
│  [编排层] LangGraph 状态机 / Planning / 多Agent协作           │
├─────────────────────────────────────────────────────────────┤
│  [能力层] LLM(混元/豆包) / Tool(工具集) / Memory(记忆)        │
├─────────────────────────────────────────────────────────────┤
│  [数据层] Redis(短期状态) / Postgres(日志) / 向量库(知识)     │
└─────────────────────────────────────────────────────────────┘
```

## 二、各层职责详解

### 1. 接入层
```
- API 网关：统一入口，协议转换（HTTP/WebSocket/SSE）
- 鉴权：API Key / OAuth / JWT
- 限流：令牌桶（防滥用、控成本）
- 请求校验：参数合法性、prompt 注入检测
```

### 2. 编排层（核心）
```
LangGraph 状态机：
  State: messages / current_plan / tool_history / step_count
  Node: intent_recognition / planning / tool_execution / aggregation / reflection
  Edge: conditional（根据状态路由）

关键设计：
  - Planning 用 Plan&Execute（先规划后执行）
  - 工具执行是确定性 Workflow
  - 反思阶段决定是否再规划
  - 硬步数上限（10-15 步）防死循环
```

### 3. 能力层
```
LLM 池：
  - 大模型（混元 pro）：总结/复杂推理
  - 小模型（混元 lite）：意图识别/简单决策（省钱）
  - 兜底模型：主模型挂了用

Tool 注册中心：
  - 工具元数据（schema/权限/超时）
  - 服务端分发（不暴露给 LLM 直接调）

Memory：
  - 短期：当前会话（Redis）
  - 长期：用户画像/历史结论（Postgres）
  - 向量：知识库（Milvus）
```

### 4. 数据层
```
Redis：会话短期状态（TTL 30min）、分布式锁、限流计数
Postgres：agent_steps 日志（可回放/审计）、用户数据
向量库（Milvus）：知识库 embedding、历史对话向量化
对象存储（COS）：文档/图片/产物
```

### 5. 可观测层（生产必备）
```
Trace：每个请求的完整链路（OpenTelemetry）
  request_id → intent → plan → tool_calls → answer
  每步记录：input/output/latency/cost

Metrics：
  - 业务：任务完成率、用户满意度
  - 技术：P99 延迟、错误率、token 消耗、工具调用成功率

Log：结构化日志（JSON），可检索

告警：
  - 错误率 > 阈值
  - P99 延迟 > 阈值
  - token 消耗异常
  - 工具失败率 > 阈值
```

## 三、生产关键关注点

### 高可用（熔断降级）
```
LLM 调用熔断：
  错误率 > 50% → 熔断 30s → 降级到兜底模型
  兜底模型也挂 → 降级到规则引擎（确定性回复）

工具调用降级：
  工具超时 → 缓存结果 / 默认值 / 跳过
  关键工具挂 → 转人工

整体降级：
  Agent 链路挂 → 退化成简单 RAG（检索+生成）
```

### 成本控制
```
Token 预算：
  每请求预算（如 10K token）
  超预算 → 强制结束 + 返回当前结果

小模型决策 + 大模型总结：
  意图识别/工具选择 → 小模型（便宜）
  最终答案生成 → 大模型（贵但质量好）

缓存：
  相同 query → 缓存结果（embedding 相似度匹配）
  相同 tool_call → 缓存结果
```

### 安全
```
工具权限分级：
  read-only → 直接放行
  write → 日志审计
  destructive → 二次确认

PII 脱敏：
  用户输入先脱敏（身份证/手机号）再喂 LLM

Prompt 注入防御：
  输入检测（"忽略以上指令"等模式）
  输出过滤（敏感词/越权内容）

审计日志：
  所有工具调用落表（谁/何时/调了什么/结果）
```

### 可演进
```
模型版本管理：
  - 模型 ID 版本化（hunyuan-pro-v1.2）
  - 新模型灰度发布（10%流量先试）
  - 可回滚

A/B 测试：
  - 实验框架（分桶/指标/显著性检验）
  - 新 prompt / 新工具 / 新模型 都能 A/B

配置中心：
  - prompt 模板、工具列表、阈值 热更新
  - 不重启服务改配置
```

## 四、从 0 到 1 的落地路径

```
[阶段1] MVP（2周）
  - 单 Agent + 3-5 个核心工具
  - 简单 RAG（向量检索）
  - 基础 trace
  - 目标：跑通核心场景

[阶段2] 生产化（4周）
  - 加熔断/降级/重试
  - 加完整可观测（metrics/log/告警）
  - 加权限/审计
  - 目标：能 7x24 稳定跑

[阶段3] 优化（持续）
  - 成本优化（小模型/缓存）
  - 质量优化（Rerank/bad case 闭环）
  - 能力扩展（多 Agent/新工具）
  - 目标：提效降本
```

## 五、加分点

- 说出 **可观测层是生产 Agent 的命脉**：没 trace 的 Agent 出问题靠猜
- 说出 **小模型决策 + 大模型总结** 的成本优化策略
- 说出 **兜底链路**：Agent 挂了退化成 RAG，RAG 挂了退化成规则，永远有答案

## 六、雷区

- ❌ "demo 跑通了就能上线" → 缺熔断/可观测，上线必挂
- ❌ "全用大模型" → 成本爆炸
- ❌ "没有兜底" → 链路任何一环挂就整个不可用

## 七、扩展

- **Multi-Region 部署**：多机房容灾，Agent 服务无状态化（状态全在 Redis/DB）
- **容量规划**：按 QPS 估算 LLM 调用量、token 消耗、向量库规模
- **SLA 定义**：可用性（99.9%）、延迟（P99 < 5s）、质量（任务完成率 > 85%）

## 记忆要点

- 五层架构：接入网关、LangGraph编排、模型与工具能力、多模态存储数据、Trace可观测
- 编排核心：状态机控制流转，Plan&Execute分离规划与执行，硬步数上限防死循环
- 生产高可用：LLM与工具需配熔断降级，大模型挂了降级小模型或缓存，最终退化为规则
- 成本与安全：小模型做大模型意图识别省钱，写入工具需二次确认防注入

