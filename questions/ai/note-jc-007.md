---
id: note-jc-007
difficulty: L3
category: ai
subcategory: 深度学习
tags:
- 阶跃星辰
- 面经
- Adam
- 优化器
- 一阶矩
- 二阶矩
feynman:
  essence: Adam 优化器结合动量（Momentum，一阶矩指数移动平均）和自适应学习率（RMSProp，二阶矩指数移动平均）。一阶矩 m_t=β1·m_{t-1}+(1-β1)·g_t（动量方向），二阶矩 v_t=β2·v_{t-1}+(1-β2)·g_t²（梯度幅度）。偏差修正后更新 θ=θ-lr·m_hat/√v_hat。本质是"用历史梯度方向（动量）加速 + 用历史梯度幅度自适应调学习率（大幅小步小步大）"。
  analogy: 像有经验的山地徒步者——一阶矩（动量）像记住之前走的方向（保持惯性，不会因一块石头就改向），二阶矩（RMSProp）像根据"这路陡不陡"调步幅（陡的地方小步稳，平的地方大步快）。两者结合就是 Adam。
  first_principle: SGD 只用当前梯度（噪声大）。Momentum 加历史方向（平滑噪声、加速）。RMSProp 加历史幅度（自适应学习率）。Adam = Momentum + RMSProp，同时利用梯度的一阶和二阶统计量。
  key_points:
  - '一阶矩 m_t = β1·m_{t-1}+(1-β1)·g_t（动量，梯度方向的EMA）'
  - '二阶矩 v_t = β2·v_{t-1}+(1-β2)·g_t²（梯度幅度的EMA）'
  - '偏差修正 m_hat=m_t/(1-β1^t), v_hat=v_t/(1-β2^t)'
  - '更新 θ=θ-lr·m_hat/(√v_hat+ε)'
  - '默认 β1=0.9, β2=0.999, ε=1e-8'
first_principle:
  essence: Adam = Momentum(一阶) + RMSProp(二阶)
  derivation: SGD噪声大 → 加历史方向(Momentum,一阶矩)平滑 → 但各维度学习率一样 → 加历史幅度(RMSProp,二阶矩)自适应 → 两者结合=Adam
  conclusion: Adam 用梯度的两个统计量（方向和幅度）做自适应更新，是深度学习默认优化器
follow_up:
- 为什么需要偏差修正？
- Adam 和 AdamW 的区别？
- 为什么 Adam 训 LLM 要用更大的 β2（如0.95）？
---

# 【阶跃星辰面经】Adam 优化器的公式：一阶矩、二阶矩、梯度更新

## 一、Adam 完整公式

```
给定：参数 θ，学习率 lr，梯度 g_t，超参 β1=0.9, β2=0.999, ε=1e-8

[1] 一阶矩（动量）更新：
    m_t = β1 · m_{t-1} + (1 - β1) · g_t
    
[2] 二阶矩（梯度幅度）更新：
    v_t = β2 · v_{t-1} + (1 - β2) · g_t²

[3] 偏差修正（修正初始偏差）：
    m_hat = m_t / (1 - β1^t)
    v_hat = v_t / (1 - β2^t)

[4] 参数更新：
    θ_t = θ_{t-1} - lr · m_hat / (√v_hat + ε)
```

## 二、一阶矩 m_t：动量（方向）

```
m_t = β1 · m_{t-1} + (1 - β1) · g_t
```

**本质**：梯度的**指数移动平均（EMA）**，代表"历史梯度的方向"。

- β1=0.9：当前梯度占 10%，历史占 90% → 平滑
- 作用：**加速收敛 + 抗噪**（单步梯度噪声被历史平均稀释）

**直觉**：像球往下滚带惯性——即使某一步梯度方向变了（噪声），球的运动方向仍由历史主导。

## 三、二阶矩 v_t：梯度幅度

```
v_t = β2 · v_{t-1} + (1 - β2) · g_t²
```

**本质**：梯度**平方**的 EMA，代表"历史梯度的幅度"。

- β2=0.999：更平滑（历史占 99.9%）
- 作用：**自适应学习率**——梯度幅度大的参数，学习率自动调小（除以大√v）；幅度小的调大

**直觉**：陡的地方（梯度大）小步走（防跨过最优点），平的地方（梯度小）大步走（加速）。

## 四、为什么要除以 √v_hat（自适应学习率）

```
更新量 = lr · m_hat / √v_hat

  m_hat：方向（带历史平滑）
  √v_hat：历史梯度幅度

某参数历史梯度一直很大（如 g≈10）：
  → v_hat≈100, √v_hat≈10
  → 更新量 = lr · 10 / 10 = lr（小步）

某参数历史梯度一直很小（如 g≈0.1）：
  → v_hat≈0.01, √v_hat≈0.1
  → 更新量 = lr · 0.1 / 0.1 = lr（其实一样？）
```

**关键**：除以 √v_hat 让每个参数的有效学习率归一化——梯度大的参数步长不被其幅度主导，而是由"方向一致性"决定。

## 五、为什么需要偏差修正

```
初始化 m_0=0, v_0=0

第一步（t=1）：
  m_1 = β1·0 + (1-β1)·g_1 = 0.1·g_1   （严重低估！真实应是 g_1）
  v_1 = β2·0 + (1-β2)·g_1² = 0.001·g_1²

偏差修正：
  m_hat = m_1 / (1-β1^1) = 0.1g_1 / 0.1 = g_1  ✓（修正回真实值）
  v_hat = v_1 / (1-β2^1) = 0.001g_1² / 0.001 = g_1²  ✓
```

**原因**：初始 m_0=v_0=0，前几步的 EMA 严重偏向 0（被初始值拖累）。偏差修正除以 (1-β^t) 补偿这个初始偏差。t 增大时 β^t→0，修正项→1，自动失效。

## 六、Adam vs SGD vs Momentum vs RMSProp

| 优化器 | 用一阶矩 | 用二阶矩 | 特点 |
|--------|---------|---------|------|
| SGD | ❌ | ❌ | 只用当前梯度，噪声大 |
| Momentum | ✅ | ❌ | 加历史方向，加速 |
| RMSProp | ❌ | ✅ | 加历史幅度，自适应 |
| **Adam** | ✅ | ✅ | **两者结合，默认选择** |

## 七、Adam 的超参与 LLM 训练

```
默认值（CV 时代）：β1=0.9, β2=0.999, lr=1e-3
LLM 训练常用：    β1=0.9, β2=0.95, lr=6e-4（GPT-3）
                 β1=0.9, β2=0.999, lr=3e-4（更稳）

为什么 LLM 用更小 β2（0.95 vs 0.999）：
  β2 小 → v_t 更关注近期梯度 → 自适应更敏感
  → LLM 训练梯度分布变化快，需要更快适应
```

## 八、AdamW（Adam 的改进）

```
Adam 的 weight decay 实现有问题（和自适应学习率耦合）：
  L2 正则加在梯度上 → 被除以 √v_hat → 各参数 weight decay 不一致

AdamW 修正：weight decay 直接作用在参数上，不和梯度耦合
  θ = θ - lr · (m_hat/√v_hat + wd · θ)
  → 各参数 weight decay 一致
  → LLM 训练标准用 AdamW
```

## 九、加分点

- 说出 **Adam = Momentum + RMSProp**（一阶+二阶）
- 说出 **偏差修正的原因**：初始 m_0=v_0=0 导致前几步低估
- 说出 **AdamW 的改进**：decoupled weight decay

## 十、雷区

- ❌ "Adam 用二阶矩就是用二阶导数" → 是梯度平方的 EMA，不是二阶导
- ❌ 忽略偏差修正 → 不修正前几步训练会偏
- ❌ "Adam 永远比 SGD 好" → SGD+Momentum 泛化更好（某些 CV 任务）

## 十一、扩展

- **学习率 warmup**：LLM 训练前几百步线性增 lr，配合 Adam 防止初期 v_t 不稳导致发散
- **Lookahead**：Adam 的 wrapper，用"快慢两个优化器"提升泛化
- **Sophia / Lion**：更新的优化器，声称比 Adam 更适合 LLM
