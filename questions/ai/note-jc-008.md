---
id: note-jc-008
difficulty: L2
category: ai
subcategory: 计算机基础
tags:
- 阶跃星辰
- 面经
- 指针
- C语言
- 内存
feynman:
  essence: 指针是"存储内存地址的变量"——它的值是另一个变量的地址。指针本身也占内存（存这个地址），占用字节数取决于系统寻址空间：32位系统4字节（寻址2^32=4GB），64位系统8字节（寻址2^64）。指针类型（int*/char*）决定"解引用时读几个字节"，但指针本身大小和类型无关，只和系统位数有关。
  analogy: 指针像一张写着"门牌号"的纸条——纸条上的内容是地址（某变量在哪），纸条本身也占地方（指针自己占内存）。门牌号长短（指针大小）取决于城市有多大（系统位数），和"这家人住的是公寓还是别墅"（int 还是 char）无关。
  first_principle: 内存是按地址访问的线性空间。指针是"地址的容器"，让程序能间接访问内存。指针大小 = 寻址所需位数 = 系统位数。
  key_points:
  - 指针 = 存储内存地址的变量，值是另一个变量的地址
  - 32位系统指针4字节(2^32寻址)，64位系统指针8字节(2^64)
  - 指针大小和类型无关(int*/char*一样大)，只和系统位数有关
  - 指针类型决定解引用读几个字节(int*读4字节,char*读1字节)
  - 解引用(*)取地址处内容，取地址(&)得变量地址
first_principle:
  essence: 指针 = 地址的容器
  derivation: 内存按地址访问 → 需要变量存地址 → 指针是存地址的变量 → 地址位数=系统位数 → 指针大小=系统位数
  conclusion: 理解指针关键是区分"指针存的值（地址）"和"指针自己占的内存"
follow_up:
- 为什么空指针（NULL）解引用会段错误？
- 函数指针和普通指针的区别？
- 指针和引用的区别？
memory_points:
- 定义：指针本身是个变量，其存储的值是另一个变量的内存地址
- 核心结论：指针大小取决于系统位数（64位=8字节），与数据类型无关
- 指针类型的作用：仅决定解引用时读取的字节数和加减法移动的步长
- 代码题陷阱：*p=*q改变指向地址的值，而p=q改变指针的指向
---

# 【阶跃星辰面经】指针的定义是什么？指针本身占用多少字节？

## 一、指针的定义

**指针**是一个变量，它的值是**另一个变量的内存地址**。

```c
int a = 42;
int *p = &a;   // p 是指针，存的是 a 的地址

// p 的值 = a 的地址（如 0x7ffeeabc）
// *p = 42（解引用，取 p 指向地址的内容）
// &a = a 的地址（取地址）
```

**关键区分**：
- `p`（指针本身）：存地址的变量
- `&a`（取地址）：得到 a 的地址
- `*p`（解引用）：取 p 指向地址的内容（= a 的值 = 42）

## 二、指针本身占用多少字节

**取决于系统位数**（寻址空间）：

| 系统 | 寻址空间 | 指针大小 |
|------|---------|---------|
| 16 位 | 2^16 = 64KB | 2 字节 |
| 32 位 | 2^32 = 4GB | **4 字节** |
| 64 位 | 2^64 = 16EB | **8 字节** |

```c
#include <stdio.h>
int main() {
    int *p;
    char *c;
    double *d;
    printf("int*   size: %zu\n", sizeof(p));   // 64位系统: 8
    printf("char*  size: %zu\n", sizeof(c));   // 64位系统: 8
    printf("double*size: %zu\n", sizeof(d));   // 64位系统: 8
    return 0;
}
```

**核心结论**：所有指针类型大小相同（64位系统都是8字节），因为它们都只是"存一个地址"，地址位数固定。

## 三、为什么指针大小 = 系统位数

```
64 位系统：
  地址总线 64 根 → 地址范围 2^64
  → 表示一个地址需要 64 bit = 8 byte
  → 指针（存地址）需要 8 字节

32 位系统：
  地址总线 32 根 → 地址范围 2^32 = 4GB
  → 表示一个地址需要 32 bit = 4 byte
  → 指针需要 4 字节
```

## 四、指针类型的作用（不是决定大小）

指针类型（int*/char*/double*）决定**解引用时读几个字节**：

```c
int a = 0x12345678;   // 4字节
int *pi = &a;
char *pc = (char*)&a;

*pi        // 读 4 字节 → 0x12345678（int 类型）
*pc        // 读 1 字节 → 0x78（char 类型，小端）

// 指针运算
pi + 1     // 移动 sizeof(int)=4 字节
pc + 1     // 移动 sizeof(char)=1 字节
```

**指针类型决定**：
1. 解引用读几个字节（int* 读 4，char* 读 1）
2. 指针加减移动多少字节（int*+1 走 4 字节）

**但指针本身大小**和类型无关（都是 8 字节 on 64bit）。

## 五、指针赋值代码题（典型考点）

```c
// 经典陷阱题
int a = 10;
int b = 20;
int *p = &a;
int *q = &b;
*p = *q;     // 等价于 a = b，a 变成 20
// 此时 a=20, b=20, p 指向 a, q 指向 b

p = q;       // p 现在指向 b（指针赋值，改的是 p 的值）
// 此时 p 和 q 都指向 b，a 仍是 20

*p = 30;     // 改 b 为 30（p 指向 b）
// 此时 a=20, b=30, p 和 q 都指向 b
```

**区分**：
- `*p = *q`：改值（a=b），指针指向不变
- `p = q`：改指针（p 指向 b），原 a 不受影响

## 六、常见指针操作

| 操作 | 含义 |
|------|------|
| `&a` | 取变量 a 的地址 |
| `*p` | 解引用，取 p 指向地址的值 |
| `p = &a` | 让 p 指向 a |
| `*p = 10` | 把 10 写入 p 指向的地址 |
| `p + 1` | 指针移动 sizeof(类型) 字节 |
| `p == q` | 比较两个指针是否指向同一地址 |
| `NULL` | 空指针（不指向任何有效地址） |

## 七、加分点

- 说出 **指针大小取决于系统位数**（64位=8字节），和类型无关
- 说出 **指针类型决定解引用读几个字节**，不是决定指针大小
- 说出 **函数指针**也是指针，大小同样 8 字节（64位），存函数入口地址

## 八、雷区

- ❌ "int* 比 char* 大" → 错，都是 8 字节（64位系统）
- ❌ 混淆 `*p = *q`（改值）和 `p = q`（改指向）
- ❌ 空指针解引用 → 段错误（访问非法地址）

## 九、扩展

- **多级指针**：`int **pp`（指向指针的指针），大小仍是 8 字节
- **指针 vs 引用**：C++ 引用是别名（不占额外内存，编译期实现），指针是实体（占内存）
- **智能指针**：C++ 的 unique_ptr/shared_ptr，本质是对象包装裸指针，自动管理生命周期

## 记忆要点

- 定义：指针本身是个变量，其存储的值是另一个变量的内存地址
- 核心结论：指针大小取决于系统位数（64位=8字节），与数据类型无关
- 指针类型的作用：仅决定解引用时读取的字节数和加减法移动的步长
- 代码题陷阱：*p=*q改变指向地址的值，而p=q改变指针的指向

